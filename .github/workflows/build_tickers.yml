name: 티커 메타데이터 수집 (1회성)

on:
  workflow_dispatch:

jobs:
  build_tickers:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 패키지 설치
        run: pip install requests pandas

      - name: 티커 수집
        env:
          EODHD_TOKEN: ${{ secrets.EODHD_TOKEN }}
        run: python build_tickers.py

      - name: 커밋 & 푸시
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add tickers_us.csv tickers_kr.csv
          git diff --staged --quiet || git commit -m "티커 메타데이터 수집 $(date +'%Y-%m-%d')"
          git push